# IAM Registry

Local IMF registry configuration for the notary domain module.

- `iam-registry.config.json`: enabled modules for notary host composition
- Materialize lock snapshots through the IMF pipeline in `sdkwork-appbase` before production rollout

See `specs/iam.module.manifest.json` for notary permission and role catalog authority.
