# SDKWork Notary App SDK Component

This SDK family owns only `sdkwork-notary.app` operations under `/app/v3/api/notary`.
IAM and Drive capabilities are consumed through dependency SDKs and composed facade ports. Merchandise, Order, and Payment remain behind the Notary service boundary and are not copied into this SDK family.
