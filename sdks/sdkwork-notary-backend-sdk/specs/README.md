# SDKWork Notary Backend SDK Component

This SDK family owns only `sdkwork-notary.backend` operations under `/backend/v3/api/notary`.
Staff and Drive management capabilities are composed through IAM and Drive dependency SDKs. Merchandise, Order, and Payment are reused behind the Notary backend orchestration boundary rather than copied into this SDK.
