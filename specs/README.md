# Specs

Root component and topology contracts for the notary domain library.

- `topology.spec.json`: v2 runtime topology profile for host application wiring (`SDKWORK_NOTARY_*` env keys)

Component-level specs live next to their owning crates and SDK families under `crates/*/specs/` and `sdks/*/specs/`.
