# Notary Schema Registry

Owned persistence contracts for the notary domain library.

- `tables/001-notary-core.yaml`: notary-owned tables, logical references, and compliance metadata

SQL migrations live in `crates/sdkwork-notary-case-repository-sqlx/`.
