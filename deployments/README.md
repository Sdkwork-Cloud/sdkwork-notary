# Deployments

Reserved for deployment manifests and release topology handoff when notary surfaces are packaged with host applications.

This domain library does not ship standalone deployment bundles. See `configs/topology/` and `docs/topology-standard.md` for development topology profiles consumed by host apps.
