specVersion = 1
kind = "sdkwork.webserver.server"
id = "notary"
description = "sdkwork-notary web server (application.public-ingress, application.backend-http)"

[nginx]
enabled = true
profile = "http-core-v1"
unknownDirectivePolicy = "error"
strict = true
confFile = "nginx.conf"

[main]
user = "sdkwork"
workerProcesses = "auto"
pid = "/run/sdkwork/notary/webserver.pid"
errorLog = "/var/log/sdkwork/notary/webserver/error.log warn"

[main.events]
workerConnections = 1024

[http]
sendfile = true
keepaliveTimeout = 75
clientMaxBodySize = "1100m"
serverTokens = "off"
gzip = true

[[http.upstream]]
name = "gateway"
loadBalancing = "least-connections"
keepalive = 32

